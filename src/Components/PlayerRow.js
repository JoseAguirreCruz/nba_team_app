import React from 'react'

const PlayerRow = ({ playersName, position, number }) => {
  return (
    <tr>
      <td>{player.name}</td>
      <td>{position}</td>
      <td>{number}</td>
    </tr>
  )
}

export default PlayerRow
